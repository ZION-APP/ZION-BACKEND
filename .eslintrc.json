{
    "env": { //Usaremos variables de de es6, node, mocha, commonjs; para que al momento de hacer test eslint no saque error
        "commonjs": true,
        "es6": true,
        "node": true,
        "mocha": true
    },
    "extends": "eslint:recommended",
    "parserOptions": {
        "ecmaVersion": 2018 //Usaremos la version 2018 en adelante
    },
    "rules": {
        "quotes": ["warn", "single"], // Para que de una alerta al no usar comillas simples
        "semi": ["warn", "always"], // Para que que de una alerta al no usar ;
        "no-console": "warn" // Para que de una alerta de los console.log() de la api
    }
}
